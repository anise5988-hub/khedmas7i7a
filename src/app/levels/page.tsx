import { WorkspacePage } from "@/components/workspace-page"; import { educationLevels } from "@/lib/domain/catalog"; export default function LevelsPage() { return <WorkspacePage eyebrow="Système éducatif tunisien" title="Ton niveau, ton rythme." description="Du primaire à l'université, retrouve un accompagnement adapté à ton programme." items={educationLevels.map((level) => level.name)} />; }